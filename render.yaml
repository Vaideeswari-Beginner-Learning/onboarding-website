services:
  - type: web
    name: onboarding-backend
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm run start
    envVars:
      - key: PORT
        value: 5000
      - key: MONGO_URI
        sync: false
