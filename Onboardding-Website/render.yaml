services:
  - type: web
    name: fic-onboarding-website
    env: node
    plan: free
    buildCommand: npm install && cd server && npm install
    startCommand: cd server && node index.js
    envVars:
      - key: PORT
        value: 5000
      - key: MONGO_URI
        sync: false
    autoDeploy: true
